import { Controller } from '@hotwired/stimulus'

export default class extends Controller {
  track(event) {
    // fbq('trackCustom', event.currentTarget.dataset.trigger);
    // event.preventDefault((e) => {
    //   console.log(event.currentTarget.dataset)
    // })
  }
}
